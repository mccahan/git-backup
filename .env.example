# Required Configuration
GIT_REPO_URL=https://github.com/yourusername/your-backup-repo.git
GITHUB_TOKEN=your_github_token_here

# Optional Configuration
GIT_BRANCH=main
BACKUP_INTERVAL_HOURS=6
GIT_USER_NAME=Git Backup Bot
GIT_USER_EMAIL=gitbackup@example.com

# Advanced: Backup to a subdirectory within the repository
# Leave empty or unset to backup to the root of the repository
# Example: REPO_SUBDIR=servers/web01
# REPO_SUBDIR=

# Web UI
# WEB_UI_PORT=3000
# BASE_PATH=/git-backup

# Data persistence paths
# CONFIG_FILE=/data/config.json
# HISTORY_FILE=/data/history.json

# Restore config from repo on fresh container (path within repo)
# CONFIG_BACKUP_PATH=git-backup-config.json
